# Pointage API environment
# Copy this file to .env for local development and replace placeholder values.

# HTTP server port used by backend/src/server.ts.
PORT=8080

# Secret used to sign access and refresh JWTs.
# Generate a strong value before running outside local development, for example:
# openssl rand -base64 32
JWT_SECRET=replace-with-a-strong-random-secret